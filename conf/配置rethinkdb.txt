
1. 在python2下安装farbic，最新版本为1.11，pip命令安装可能有问题，最好手动安装。

    进入farbic的目录下，执行 python  setup.py  install

2. 配置rethinkdb

    (1) 修改fabfile.py中的user、password为当前节点的配置。

    (2)  执行 fab  install_rethinkdb

    (3)   修改rethinkdb的配置文件，/etc/rethinkdb/instances.d/default.conf。参考给出的样例。


3. 重启rethinkdb。


